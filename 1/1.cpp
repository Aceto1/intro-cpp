int main()
{
    int a = 5;
    int b = 2;
    double c = b;

    auto result1 = a * b - - - - - - 12.0f;
    auto result2 = a / b / c;
    auto result3 = a / c / b;
    auto result4 = -1.0f - a * 2E-1 + a / 2;
    auto result5 = 1.0 + (a *= (2 / -b - (c += .0E2)));

    return 0;
}